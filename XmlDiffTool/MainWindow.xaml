<Window x:Class="XmlDiffTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="XML Diff Tool" Height="360" Width="760"
        MinHeight="320" MinWidth="680">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="XML Diff HTML Report"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Margin="0,0,0,14" />

            <Border Grid.Row="1"
                    Padding="14"
                    BorderThickness="1"
                    BorderBrush="#FFD6D6D6"
                    CornerRadius="6"
                    Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Text="Left XML"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold" />
                    <TextBox Grid.Row="0"
                             Grid.Column="1"
                             Height="28"
                             Margin="0,0,8,8"
                             Text="{Binding LeftFilePath, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="True" />
                    <Button Grid.Row="0"
                            Grid.Column="2"
                            Content="Browse..."
                            Width="96"
                            Margin="0,0,0,8"
                            Command="{Binding BrowseLeftCommand}" />

                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               Text="Right XML"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold" />
                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             Height="28"
                             Margin="0,0,8,8"
                             Text="{Binding RightFilePath, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="True" />
                    <Button Grid.Row="1"
                            Grid.Column="2"
                            Content="Browse..."
                            Width="96"
                            Margin="0,0,0,8"
                            Command="{Binding BrowseRightCommand}" />

                    <StackPanel Grid.Row="2"
                                Grid.Column="1"
                                Grid.ColumnSpan="2"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <CheckBox IsChecked="{Binding IgnoreCaseValues}"
                                  VerticalAlignment="Center">
                            <TextBlock Text="Ignore case for tags, attributes, and values"
                                       VerticalAlignment="Center" />
                        </CheckBox>
                    </StackPanel>
                </Grid>
            </Border>

            <TextBlock Grid.Row="2"
                       Text="HTML reports include Bootstrap styling, side-by-side values, expandable nodes, and report-level filters for left-only and right-only elements."
                       TextWrapping="Wrap"
                       Foreground="#FF606060"
                       Margin="0,16,0,0" />

            <Grid Grid.Row="3" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding ResultSummary}"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0" />
                <Button Grid.Column="1"
                        Content="Generate HTML Report"
                        Width="170"
                        Height="34"
                        HorizontalAlignment="Right"
                        Command="{Binding CompareCommand}" />
            </Grid>
        </Grid>

        <Border Margin="10"
                Background="#80FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                Panel.ZIndex="1">
            <Border Width="320"
                    Padding="20"
                    Background="White"
                    BorderBrush="#FFD0D0D0"
                    BorderThickness="1"
                    CornerRadius="8"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="{Binding ProgressText}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center" />
                    <ProgressBar Margin="0,16,0,0"
                                 Height="18"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="{Binding ProgressPercentage, Mode=OneWay}" />
                    <TextBlock Text="{Binding ProgressPercentage, StringFormat={}{0}%}"
                               Margin="0,12,0,0"
                               HorizontalAlignment="Center"
                               FontWeight="Medium" />
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
